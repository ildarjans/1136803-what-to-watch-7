import PropTypes from 'prop-types';

export const funcPropTypes = PropTypes.func;

export const stringPropTypes = PropTypes.string;

export const numberPropTypes = PropTypes.number;

export const booleanPropTypes = PropTypes.bool;

export const nodePropTypes = PropTypes.node;
